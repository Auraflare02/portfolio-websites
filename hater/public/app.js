const message=document.querySelector('#message'), count=document.querySelector('#count'), button=document.querySelector('#submit'), status=document.querySelector('#status');
message.addEventListener('input',()=>count.textContent=`${message.value.length} / 500`);
button.addEventListener('click',async()=>{const text=message.value.trim();if(text.length<3){status.textContent='Write at least a few words.';return}button.disabled=true;button.textContent='SENDING…';try{const r=await fetch('/api/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,website:document.querySelector('#website').value})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not send');message.value='';count.textContent='0 / 500';status.textContent='Sent. Your identity was not requested.';}catch(e){status.textContent=e.message||'Try again.'}finally{button.disabled=false;button.innerHTML='SEND ANONYMOUSLY <b>↗</b>'}});
